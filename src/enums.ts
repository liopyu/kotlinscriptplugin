export enum RangeMode {
    MODIFIED,
    SCOPE,
    FULL,
    EDIT,
    REPROCESSING
}
export enum VariableType {
    SIMPLE,
    VARIABLE,
    IMPORT,
    METHOD
}

export enum VariableParameter {
    FUNCTION_VALUE_PARAMETERS,
    LAMBDA_PARAMETERS,
    CATCH_BLOCK

}